apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "starship-ai.fullname" . }}-test-connection"
  labels:
    {{- include "starship-ai.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "starship-ai.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
