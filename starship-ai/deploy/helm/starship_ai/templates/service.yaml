apiVersion: v1
kind: Service
metadata:
  name: {{ include "starship-ai.fullname" . }}
  labels:
    {{- include "starship-ai.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "starship-ai.selectorLabels" . | nindent 4 }}
